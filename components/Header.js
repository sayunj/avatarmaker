import {Layout, Row, Col} from 'antd';
const {Header} = Layout;

export default () => (
    <Header style={{backgroundColor: '#fff'}}>
        <Row>
            <header class='container'>
                AvatarMaker
            </header>
        </Row>
    </Header>
)